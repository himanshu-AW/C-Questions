#include<stdio.h>
#include<conio.h>
#define n 50
void main ()
{ int i,**p,*q,name[20],cls,rol,fee;
int a[n];
    printf("Enter the stdent detail\n");
    for(i=0;i<n;i++)
    {
        printf("\nstudent no %d",i+1);
        printf("Enter the name : "); scanf("%s",a[i].name);
    }
getch();

}